var age = prompt("what is your age? ");
do {
    document.write(age);
    var user = prompt("should i continue? ");
  user = user.toUpperCase();
  var age = prompt("what is your age? ");
} while (response == "yes");
